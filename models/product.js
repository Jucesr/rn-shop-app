class Product {
   constructor(id, ownerId, name, image, description, unit_rate) {
      this.id = id;
      this.ownerId = ownerId;
      this.name = name;
      this.image = image;
      this.description = description;
      this.unit_rate = unit_rate;
   }
}

export default Product;
